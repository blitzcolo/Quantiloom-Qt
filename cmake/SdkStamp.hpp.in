// Generated by CMake from cmake/SdkStamp.hpp.in -- do not edit.
//
// Records which Quantiloom SDK this binary was compiled and linked against, so
// src/SdkGuard.cpp can refuse to run against a different one. See the "SDK
// freshness guard" block in CMakeLists.txt for why this exists.

#pragma once

namespace quantiloom_qt::sdk_stamp {

// SDK root this build tree was configured against (forward slashes).
inline constexpr const char* kSdkDir = "@QUANTILOOM_SDK_DIR@";

// Full path to the SDK's Quantiloom shared library at configure time. Empty on
// platforms where the imported target exposes no IMPORTED_LOCATION.
inline constexpr const char* kSdkLibraryPath = "@QUANTILOOM_SDK_DLL@";

// Bare file name, e.g. "Quantiloom.dll". This is the copy the executable
// actually loads, since it sits next to the executable.
inline constexpr const char* kSdkLibraryName = "@QUANTILOOM_SDK_DLL_NAME@";

// SHA-256 of that library as it was when this binary was built. Empty if CMake
// could not read the file, in which case the guard stays silent.
inline constexpr const char* kSdkLibrarySha256 = "@QUANTILOOM_SDK_DLL_SHA256@";

// Version reported by find_package(Quantiloom), for the diagnostic text.
inline constexpr const char* kSdkVersion = "@Quantiloom_VERSION@";

} // namespace quantiloom_qt::sdk_stamp
