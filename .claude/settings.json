{
  "permissions": {
    "deny": [
      "Read(./assets/models/**)",
      "Read(./assets/maps/**)",
      "Read(./assets/spectral/*.json)",
      "Read(./assets/spectral/*.qlbin)",
      "Read(./assets/luts/*.bin)"
    ],
    "additionalDirectories": [
      "/mnt/h/Quantiloom-SDK",
      "/mnt/h/Quantiloom-dev",
      "/mnt/c/Qt"
    ]
  },
  "enabledPlugins": {
    "clangd-lsp@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR:-.}\"/scripts/hooks/post-write-cdb-staleness.sh",
            "timeout": 30
          }
        ]
      }
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Building this project with cmake/cmake.exe (configure, build) in build-msvc, build, or build-llvm, including running build_wsl.sh or build_windows_msvc.ps1",
      "Running QuantiloomQt.exe or other binaries from the build trees with any arguments",
      "Running Qt tooling (windeployqt, moc, rcc, uic, qmake) from the Qt install",
      "Reading, listing, and inspecting files anywhere under /mnt/h/Quantiloom-Qt, /mnt/h/Quantiloom-SDK, /mnt/h/Quantiloom-dev, and the Qt install at /mnt/c/Qt",
      "Read-only git operations (status, log, diff, show, branch, blame)",
      "Creating or overwriting temporary test files and build artifacts inside this repository"
    ]
  }
}
